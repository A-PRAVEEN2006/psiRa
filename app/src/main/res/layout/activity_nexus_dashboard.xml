<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0D0D0D"
    android:padding="24dp">

    <TextView
        android:id="@+id/nexusTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="PsiRa NEXUS HUB"
        android:textColor="#B4BC22"
        android:textSize="28sp"
        android:textStyle="bold"
        android:layout_marginTop="40dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <TextView
        android:id="@+id/nexusSubTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="SELECT COMMUNICATION PROTOCOL"
        android:textColor="#4D4D4D"
        android:textSize="12sp"
        app:layout_constraintTop_toBottomOf="@id/nexusTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <Button
        android:id="@+id/btnGlobal"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:text="🌍 GLOBAL PROTOCOL\n(PUBLIC CHANNEL)"
        android:backgroundTint="#1A1A1A"
        android:textColor="#00FF00"
        android:layout_marginTop="50dp"
        app:layout_constraintTop_toBottomOf="@id/nexusSubTitle" />

    <Button
        android:id="@+id/btnWalkie"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:text="📻 WALKIE-TALKIE\n(7-DIGIT FREQUENCY)"
        android:backgroundTint="#1A1A1A"
        android:textColor="#AA14D9"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/btnGlobal" />

    <Button
        android:id="@+id/btnPrivate"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:text="🔒 GHOST VAULT\n(ENCRYPTED PRIVATE STORAGE)"
        android:backgroundTint="#1A1A1A"
        android:textColor="#C80A0A"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/btnWalkie" />

</androidx.constraintlayout.widget.ConstraintLayout>